<p id="notice"><%= notice %></p>

<% if notice %>
<p class="url">Share this link: <strong>http://cinewi.sh<%= request.fullpath %></strong></p>
<% else %>
<p><%= @wish.twitter %> wants to see <strong><%= @wish.film %></strong> with someone.</p>

<hr />
<p>
  Yeah, go on. How about this showing?
  <select name="wish[film]">  
    <% @showings.each do |showing| %>
      <% if @wish.film == showing.film %>
      <option value="<%= showing.datetime %>"><%= showing.datetime.strftime("%l:%M%P on %d/%m/%Y") %><option>
      <% end %>
    <% end %>
  </select>
</p>

<textarea placeholder="Anything else you wanna say.."></textarea>

<button type="submit">How's that?</button>
<% end %>